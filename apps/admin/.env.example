# Copy to .env.local. Next.js loads env from this directory, not the repo root.

# Server-side base URL for the LIT API.
API_URL=http://localhost:3000

# Same value, exposed to the browser bundle.
NEXT_PUBLIC_API_URL=http://localhost:3000
